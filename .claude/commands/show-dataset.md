# /show-dataset — View conversations in a dataset

Run immediately without any reasoning or reformatting:

```bash
python show_dataset.py $ARGUMENTS
```

Just execute the command and print the raw output. Do not summarize, reformat, or add commentary.
